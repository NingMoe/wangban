function init(){
	/*暂时屏蔽
 	var cmd = new LEx.Command("app.pmi.content.ChannelCmd");
 	cmd.setParameter("PARENT", "govopen");
 	cmd.setParameter("start", "0");
 	cmd.setParameter("limit", "8");
	var ret =  cmd.execute("getGovopenChannelList");
	if(!ret.error){
		$("#govopen_list").html(LEx.processDOMTemplate("govopen",ret));
	}else{
		LEx.alert(ret.error);
	}

	var cmd = new LEx.Command("app.pmi.content.ChannelCmd");
 	cmd.setParameter("PARENT", "govinvest");
 	cmd.setParameter("start", "0");
 	cmd.setParameter("limit", "5");
	var ret =  cmd.execute("getGovopenChannelList");
	if(!ret.error){
		$("#govinvest_list").html(LEx.processDOMTemplate("govinvest",ret));
	}else{
		LEx.alert(ret.error);
	}
  */	 
}

